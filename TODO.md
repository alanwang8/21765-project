# To-do list (per-member template)

_Use one file per teammate if required; this file is a shared stub._

- [ ] Confirm CUDA build on target GPU (driver / toolkit versions).
- [ ] Re-run full Python `pytest` after local dependency install.
- [ ] Optional: tune `bench_flash` sizes for demo hardware.
